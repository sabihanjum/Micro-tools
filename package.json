{
  "name": "quick-split",
  "version": "1.0.0",
  "description": "A simple bill splitting calculator with tip calculation",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest --run",
    "test:watch": "vitest",
    "deploy:netlify": "npm run build && netlify deploy --prod",
    "deploy:vercel": "npm run build && vercel --prod"
  },
  "keywords": [
    "bill-splitter",
    "tip-calculator"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "fast-check": "^3.15.0",
    "jsdom": "^27.2.0",
    "vite": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
